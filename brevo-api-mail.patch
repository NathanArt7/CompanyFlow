diff --git a/backend-reservation/app/Providers/AppServiceProvider.php b/backend-reservation/app/Providers/AppServiceProvider.php
index 1df81cd..d994be3 100644
--- a/backend-reservation/app/Providers/AppServiceProvider.php
+++ b/backend-reservation/app/Providers/AppServiceProvider.php
@@ -4,9 +4,12 @@
 
 use Illuminate\Cache\RateLimiting\Limit;
 use Illuminate\Http\Request;
+use Illuminate\Support\Facades\Mail;
 use Illuminate\Support\Facades\RateLimiter;
 use Illuminate\Support\ServiceProvider;
 use Illuminate\Support\Str;
+use Symfony\Component\Mailer\Bridge\Brevo\Transport\BrevoTransportFactory;
+use Symfony\Component\Mailer\Transport\Dsn;
 
 class AppServiceProvider extends ServiceProvider
 {
@@ -33,5 +36,22 @@ public function boot(): void
             return Limit::perMinute(5)->by($key);
 
         });
+
+        // Envoi des e-mails via l'API HTTP de Brevo plutôt que par SMTP :
+        // certains hébergeurs (dont Render) bloquent ou rendent instable le SMTP
+        // sortant, ce qui provoquait des requêtes bloquées ~60s (timeout socket
+        // PHP) sur toute action déclenchant un envoi de mail (inscription, etc.).
+        // L'API HTTP passe par HTTPS, qui n'est jamais filtré.
+        Mail::extend('brevo', function () {
+
+            return (new BrevoTransportFactory())->create(
+                new Dsn(
+                    'brevo+api',
+                    'default',
+                    config('services.brevo.key')
+                )
+            );
+
+        });
     }
 }
diff --git a/backend-reservation/config/mail.php b/backend-reservation/config/mail.php
index e32e88d..e01c72f 100644
--- a/backend-reservation/config/mail.php
+++ b/backend-reservation/config/mail.php
@@ -53,6 +53,10 @@
             'transport' => 'ses',
         ],
 
+        'brevo' => [
+            'transport' => 'brevo',
+        ],
+
         'postmark' => [
             'transport' => 'postmark',
             // 'message_stream_id' => env('POSTMARK_MESSAGE_STREAM_ID'),
diff --git a/backend-reservation/config/services.php b/backend-reservation/config/services.php
index 6a90eb8..6f9dcfb 100644
--- a/backend-reservation/config/services.php
+++ b/backend-reservation/config/services.php
@@ -22,6 +22,10 @@
         'key' => env('RESEND_API_KEY'),
     ],
 
+    'brevo' => [
+        'key' => env('BREVO_API_KEY'),
+    ],
+
     'ses' => [
         'key' => env('AWS_ACCESS_KEY_ID'),
         'secret' => env('AWS_SECRET_ACCESS_KEY'),
